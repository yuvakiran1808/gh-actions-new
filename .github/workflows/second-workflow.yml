name: my second workflow file

on:
  branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build and Test
        run: echo "Building project..."
  
  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment: dev
    steps:
      - name: Deploy to dev
        run: echo "Deploying to dev..."




